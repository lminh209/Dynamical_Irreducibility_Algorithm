load "/Users/leducanhminh/Desktop/ResearchProjectWithTori/Week 4/Helper_Functions.txt";
load "/Users/leducanhminh/Desktop/ResearchProjectWithTori/Week 6/Algorithm1/Algorithm_v4.txt";

p:=5; // change prime p here
s:=2; // change index s of p here
F:=FiniteField(p^s);
S<x>:=PolynomialRing(F);
n:=p^s-1;
P:=[];

// Print the result for all polynomials (CURRENT TEST)

for f in [x^3 + b*x + c : b in [1..n], c in [1..n]] do
    // f;
    Algorithm1(f, p, 3, s);
end for;


// Print DI polynomials specifically
/*
for f in [x^3 + b*x + c : b in [1..n], c in [1..n]] do
    if Algorithm1(f, p, 3, s) eq true then
        // Parent(f);
        P:=Append(P, f);
    // elif Algorithm1(f, p, 3, s) eq -1 then
    end if;
end for;
P;
*/

// Test for specific polynomial (uncomment to run)
/*
h:=x^3+x+1;
Algorithm1(h, p, 3, s); */



